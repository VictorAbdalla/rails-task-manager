<h1>Task</h1>
<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to 'back to the full list', tasks_path %>
<%= link_to 'edit this task', edit_task_path(@task) %>
<%= link_to 'Delete Task', task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
